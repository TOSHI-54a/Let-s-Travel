<%= form_with model: @comment, url: comment_path(@comment) do |f| %>
    <%= f.label '本文' %>
    <%= f.text_area :body, class: 'form-control mb-3', row: '4', placeholder: @comment.body %>
    <%= f.submit '編集', class: 'btn btn-primary' %>
<% end %>